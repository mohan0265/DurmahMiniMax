# ==========================================
# DURMAH CLIENT CONFIGURATION (Netlify/Vite)
# ==========================================
# Important: Vite reads only variables prefixed with VITE_
# Netlify should store these as Environment Variables

# === Frontend -> backend base URL (Render) ===
VITE_API_BASE=https://your-render-service.onrender.com

# === OpenAI / Realtime (browser flow when using direct WebRTC to provider) ===
# If the client requests an ephemeral key from your server, it should hit:
VITE_SESSION_ENDPOINT=/api/realtime/session
VITE_REALTIME_MODEL=gpt-4o-realtime-preview-2024-12-17

# === ElevenLabs (frontend should NOT hold the key; keep calls server-side) ===
# Leave blank; the client streams audio returned by the server or remote peer:
VITE_TTS_PROVIDER=elevenlabs

# === Supabase (client needs anon + URL) ===
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# === WebRTC / ICE (optional if you want client to know TURN as well) ===
VITE_TURN_URL=turn:your-turn.example.com:3478
VITE_TURN_USERNAME=turn-user
VITE_TURN_CREDENTIAL=turn-pass

# === UI / Behavior toggles ===
VITE_APP_NAME=Durmah
VITE_DEBUG_VOICE=false
VITE_MAX_SESSION_MINUTES=10

# === Development Settings ===
VITE_DEV_MODE=false
VITE_LOCAL_API=http://localhost:3001

# === Voice Settings ===
VITE_DEFAULT_VOICE=alloy
VITE_SAMPLE_RATE=24000
VITE_ENABLE_CAPTIONS=true
VITE_AUTO_START_VOICE=false

# === Accessibility Settings ===
VITE_HIGH_CONTRAST=false
VITE_LARGE_TEXT=false
VITE_KEYBOARD_NAV=true

# === Feature Flags ===
VITE_ENABLE_WIDGET_MODE=true
VITE_ENABLE_FULLSCREEN_MODE=true
VITE_ENABLE_MOBILE_OPTIMIZATIONS=true