# ==========================================
# DURMAH SERVER CONFIGURATION (Render)
# ==========================================

# === OpenAI Realtime (server side, for token minting / SDP proxy if used) ===
OPENAI_API_KEY=sk-xxxx
REALTIME_API_BASE=https://api.openai.com
REALTIME_MODEL=gpt-4o-realtime-preview-2024-12-17

# === ElevenLabs (server-side TTS call) ===
ELEVENLABS_API_KEY=elevenlabs_xxx
ELEVENLABS_VOICE_ID=EXAVITQu4vr4xnSDxMaL

# === Supabase (server; service role stays server-only) ===
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
# Optional if server also needs anon for certain flows:
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# === WebRTC / TURN (optional but recommended for NAT) ===
TURN_URL=turn:your-turn.example.com:3478
TURN_USERNAME=turn-user
TURN_CREDENTIAL=turn-pass

# === CORS / Origins (Render backend) ===
ALLOWED_ORIGINS=https://your-netlify-site.netlify.app,https://www.mydurhamlaw.com,http://localhost:5173

# === Server config (Render provides PORT automatically) ===
NODE_ENV=production
LOG_LEVEL=info
# If you expose a healthcheck:
HEALTHCHECK_PATH=/api/healthz

# === App defaults for session minting ===
DEFAULT_VOICE=alloy
DEFAULT_SAMPLE_RATE=24000

# === Rate Limiting ===
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# === Session Management ===
JWT_SECRET=generate_32_character_random_string_here
SESSION_SECRET=another_32_character_random_string_here

# === Debug Flags ===
DEBUG_VOICE=false
VOICE_LOOPBACK=false